# render/

pdfjs-dist wrapper: page canvas + text layer, viewport/projection. Single
source of the rendered page box (AD-4). Never knows about annotations.

Empty placeholder in Story 1.1 — built starting Story 1.3.
