# openapi-typescript@7 declares a peer of typescript@^5, but the project pins
# TypeScript 6.0 (ARCHITECTURE-SPINE AD-2). TS 6 works with it; relax the peer
# check so installs resolve. Remove once openapi-typescript widens its peer range.
legacy-peer-deps=true
