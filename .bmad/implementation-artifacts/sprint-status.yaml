# generated: 2026-06-28
# last_updated: 2026-07-05 (added Library epics 6-8 from epics.md: Epic 6 + 7 decomposed into backlog stories, Epic 8 remote-sync DEFERRED/not decomposed; epics 1-5 preserved terminal, action_items carried over unchanged)
# project: Paper Mate
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/.bmad/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#   - blocked: Attempted and reverted due to a hard external blocker (not merely
#     unstarted); descoped from the current epic/version. See deferred-work.md
#     for the writeup. An epic with a `blocked` story can still reach 'done'.
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-06-28
last_updated: "2026-07-12 (story 8-11-snap-empty-space-drag-to-text-attempt-2: created backlog -> ready-for-dev, sequenced before 8-10)"
project: Paper Mate
# 2026-06-28: epic-1 in-progress, story 1-1 done (PR #1 merged), story 1-2 done (reviewed), story 1-3 ready-for-dev
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/.bmad/implementation-artifacts"

development_status:
  # Epic 1: Read a paper
  epic-1: done
  1-1-walking-skeleton-app-shell: done
  1-2-open-a-pdf-from-disk: done
  1-3-render-pages: done
  1-4-scroll-and-page-navigation: done
  1-5-zoom: done
  # 2026-06-28 correct-course: inserted render-fix stories as 1-6/1-7 and
  # renumbered the original pan/toc stories to 1-8/1-9 so number = exec order.
  # (Neither pan nor toc had a story file or code under the old numbers.)
  1-6-pdfjs-decoder-assets: done
  1-7-render-perf-windowing: done
  1-8-pan-hand-tool: done
  1-9-table-of-contents: done
  epic-1-retrospective: done

  # Epic 2: Annotate the paper
  # 2026-06-29 correct-course (sprint-change-proposal-2026-06-29.md): added a
  # dev-infra enabler (2-1) + split the foundation out of the old 2-1 into 2-2,
  # renumbering the six tool stories to 2-3..2-9 so number = exec order.
  epic-2: done
  2-1-dev-infra-enabler: done
  2-2-annotation-foundation: done
  2-3-highlight-text-via-drag: done
  # 2026-06-29 correct-course (sprint-change-proposal-2026-06-29-tool-fsm.md):
  # Story 2.3 live smoke surfaced two design changes. Inserted a tool-state FSM
  # refactor (2-4) + arm-time color pick (2-5) ahead of the remaining tool
  # features, renumbering the old 2-4..2-9 to 2-6..2-11 (number = exec order).
  # 2026-06-29 correct-course (sprint-change-proposal-2026-06-29-select-highlight.md):
  # Story 2.3 live smoke also found highlights are not selectable (no recolor/
  # delete after creation) and Epic 3's 3.1/3.3 silently assume a selection seam
  # nobody builds. Inserted Story 2-5 (select-highlight: click-select + recolor +
  # delete, AD-12) right after the FSM, renumbering the old 2-5..2-11 to 2-6..2-12.
  2-4-unify-tool-state-fsm: done
  2-5-select-highlight-recolor-delete: done
  2-6-arm-time-color-pick: done
  2-7-underline-text: done
  2-8-pen-freehand: done
  2-9-textbox-memo: done
  2-10-comment-highlight-pin-bubble: done
  2-11-box-select-a-region: done
  2-12-drag-to-change-tool-quick-box: done
  # 2026-06-29 correct-course (user feature request): pen stroke alpha. Appended as
  # 2-13 (after the tool stories) so 2-9..2-12 are not renumbered.
  2-13-pen-alpha: done
  epic-2-retrospective: done

  # Epic 3: Edit, persist & review
  epic-3: done
  3-1-edit-annotations-command-path: done
  3-2-undo-redo: done
  3-3-delete-annotation: done
  3-4-autosave-to-disk: done
  3-5-restore-on-reopen: done
  3-6-annotation-bank: done
  # 2026-06-30 correct-course (sprint-change-proposal-2026-06-30.md): folded two
  # deferred-work edit capabilities onto the Epic-3 command path (post-v1, FR-26/27).
  3-7-convert-highlight-comment: done
  # 2026-07-02: attempted in full, discarded on a hard Chromium caretRangeFromPoint/
  # caretPositionFromPoint blocker during live smoke (deferred-work.md has the full
  # writeup); descoped from v1 in the Epic-3 retro. epic-3 closes without it.
  3-8-adjust-text-range: blocked
  epic-3-retrospective: done

  # 2026-06-30 correct-course (sprint-change-proposal-2026-06-30.md): grouped the
  # deferred-work polish/quality items into two POST-v1 (Phase-1.5) epics.
  # Epic 4: Reading & annotation fidelity (render/anchor correctness + on-page treatment)
  epic-4: done
  4-1-text-layer-copy-selection-fidelity: done
  4-2-column-aware-selection-highlight: done
  # 2026-07-02: DESCOPED from v1 (product decision, never attempted — no story
  # file, no code). Marked blocked (not the 3.8 attempted-and-reverted flavor) so
  # epic-4 can still reach done after 4-2 merges. See deferred-work.md "Descoped:
  # Story 4.3" + the epics.md Story 4.3 section (kept, marked descoped).
  4-3-distinct-on-page-mark-treatment: blocked
  epic-4-retrospective: optional

  # Epic 5: Reader preferences & polish (refactor enabler + preferences + UX polish)
  epic-5: done
  # 5-0 structural refactor: ideally pulled to the Epic-2/3 boundary (before 3.1).
  5-0-structural-refactor: done
  5-1-settings-hotkey-rebinding: done
  # 2026-07-02: DESCOPED from v1 (product decision, never attempted — no story
  # file, no code). Marked blocked (not the 3.8 attempted-and-reverted flavor)
  # so epic-5 can still reach done without it. See deferred-work.md "Descoped:
  # Story 5.2" + the epics.md Story 5.2 section (kept, marked descoped).
  5-2-color-system-per-tool-custom: blocked
  # 2026-07-02: user request (React client structural refactor: Reader.tsx/
  # AnnotationLayer.tsx/AnnotationInteraction.tsx bloated post-5.0, 2293
  # combined lines -- modularize, dedupe, remove dead code). Inserted as 5-3,
  # shifting the prior 5-3..5-6 down to 5-4..5-7 (epics.md updated to match).
  # 2026-07-02: implemented directly from an ad-hoc plan (no bmad-create-story
  # run, no dedicated story file -- epics.md Story 5.3 + PR #36's description
  # stand in for it). Codex code review clean (0 findings). review -> done at merge.
  5-3-client-structural-refactor-reader-annotation: done
  # 2026-07-02: user request (React client src/ folder-structure refactor:
  # client/src/ root has 38 flat .tsx/.ts/.css/.test files beside the layer
  # dirs -- adopt the /scaffold-react folder layout adapted to Vite+TS+Zustand,
  # colocate each component with its css+test, home the hooks/leaves, keep only
  # entry/config at root). Inserted as 5-4, shifting the prior 5-4..5-7 down to
  # 5-5..5-8 (epics.md updated to match; 5-8-doc-scope-store stays sprint-only,
  # not yet written in epics.md). Pure refactor thread, same footing as 5-0/5-3.
  5-4-client-src-modularize: done
  5-5-hide-show-all-annotations: done
  # 2026-07-03: rescoped to layered-Esc-ONLY (user decision). The in-editor
  # confirm-check affordance + the collapsed pen stroke-width dropdown are
  # DISCARDED (not deferred). Story file + epics.md Story 5.6 note record it.
  5-6-interaction-polish-esc-confirm-strokewidth: done
  # 2026-07-03: DESCOPED from v1 (product decision, never attempted -- no story
  # file, no code). Marked blocked (not the 3.8 attempted-and-reverted flavor)
  # so epic-5 can still reach done without it. See deferred-work.md "Descoped:
  # Story 5.7" + the epics.md Story 5.7 section (kept, marked descoped).
  5-7-dim-toc-panel: blocked
  # 2026-07-02 correct-course (sprint-change-proposal-2026-07-02.md): AE-4/AE3-3
  # (doc-scope the store) routed to its own story rather than a grooming-pass
  # hand-edit -- store owns (docId, annotations) atomically, autosave binds to
  # store.docId, the useAutosave generationRef cross-doc guard retires. Needs a
  # doc-switch DPR>1 live smoke (AE-5). Run bmad-create-story next.
  5-8-doc-scope-store: done
  epic-5-retrospective: optional

  # === Library run (Phase 2) — added 2026-07-05 from epics.md ===
  # Source: prd-paper-mate-library-2026-07-04 + architecture-paper-mate-library-2026-07-04.
  # No story files created yet (bmad-create-story not run) -> all backlog.

  # Epic 6: The library becomes home (router flip + metadata-extraction domain
  # layer + concurrency-safe library.json index; Phase-2 risk gate)
  epic-6: done
  6-1-router-flip-library-shell: done
  6-2-collection-index: done
  6-3-collection-table-view: done
  6-4-bulk-upload-optimistic-rows: done
  6-5-metadata-extraction: done
  6-6-inline-edit-title-authors: done
  6-7-open-paper-in-annotator: done
  # 2026-07-05 correct-course (sprint-change-proposal-2026-07-05-epic-6-refactor.md):
  # user request -- Epic 6 maintainability refactor. Split the 621-line storage
  # god-module + the extract/enrich domain (Crossref behind a port) + dedupe the
  # docs.py error-envelope/exception mapping, and modularize the client library/
  # into the scaffold-react layout. Pure refactor thread, same footing as
  # 5-0/5-3/5-4; its own PR(s). Appended as 6-8 (no renumbering; 6-7 is last).
  # Run bmad-create-story next.
  6-8-epic-6-structural-refactor: done
  epic-6-retrospective: done

  # Epic 7: Organize & curate the collection (folders, batch move, sort/filter,
  # Trash lens, reserved Note file-type). Builds on Epic 6's table + index.
  epic-7: done
  7-1-folders-crud-nest: done
  7-2-assign-filter-by-folder: done
  7-3-multi-select-batch-move: done
  7-4-display-sort-filter-controls: done
  7-5-trash-soft-delete-restore-purge: done
  # 2026-07-07 correct-course (sprint-change-proposal-2026-07-07.md): user
  # dropped the Note file-type and added Recent + Starred views in its place.
  # 7-6 descoped -- never attempted, no story file, no code; the reserved
  # `file_type: "note"` enum value from Epic 6 stays but nothing displays/creates
  # a note this sprint. Marked blocked (not attempted-and-reverted) so epic-7
  # can still reach done. LFR-17 defers to a future notes epic.
  7-6-note-file-type: blocked
  # New Epic-7 work, replaces 7-6. Both light up the inert Recent/Starred
  # left-panel placeholders from Story 7.1. No story files yet -> backlog.
  7-7-recent-view: done
  7-8-starred-papers: done
  # 2026-07-07 correct-course (sprint-change-proposal-2026-07-07-metadata-columns.md):
  # user request -- add Venue / Year / DOI columns to the collection table.
  # Full-stack additive: new DocMeta/CollectionRow fields + a Crossref-capture
  # extension (container-title -> Venue, issued -> Year; DOI persisted) + three
  # sortable/hideable client columns. Crossref new-imports-only (no backfill of
  # existing papers). Builds on Story 7.4's column model. No story file yet -> backlog.
  7-9-venue-year-doi-columns: done
  # 2026-07-08 correct-course (sprint-change-proposal-2026-07-08-refactor-resequence.md):
  # user request -- move the Epic 7 structural refactor to the LAST story of the
  # epic so it absorbs the debt from EVERY feature story, and expand its scope to
  # the whole Epic 7 surface. Renumbered (number = exec order):
  #   old 7-11 reorder-columns-persisted  -> 7-10
  #   old 7-12 author-tag-column          -> 7-11
  #   old 7-10 epic-7-structural-refactor -> 7-12 (now last)
  # 7-10: drag-reorder columns; persist order + visibility + widths in a client-only
  # localStorage table-view-prefs store (first persisted table layout; row order +
  # library.json untouched). No story file yet -> backlog.
  7-10-reorder-columns-persisted: done
  # 7-11: a column cell-type seam with Author as the first "tag" column -- authors
  # surfaced as a first-class list (additive contract + meta.json schema decision),
  # editable add/remove chips, filter-by-tag; colors deferred. Introduces the
  # cell-type dispatch seam that 7-12 later consolidates. No story file yet -> backlog.
  7-11-author-tag-column: done
  # 7-12: Epic 7 structural refactor, now the LAST story so it refactors the WHOLE
  # epic surface (incl. 7.9 columns + 7.10 prefs store + 7.11 cell-type/tag seam):
  # decompose CollectionTable + LibraryPage + the near-twin op hooks (client) and
  # library_index.py mutators + routes/library.py set-based handlers (backend) into
  # cohesive single-responsibility modules; audit deps, dedupe, simplify. Pure
  # refactor thread, same footing as 5-0/5-3/5-4/6-8; behavior/contract-identical,
  # own PR(s). No story file yet -> backlog.
  7-12-epic-7-structural-refactor: done
  epic-7-retrospective: done

  # Remote sync — DEFERRED, UN-NUMBERED (was epic-8) as of 2026-07-11 correct-course
  # (sprint-change-proposal-2026-07-11-deprioritize-sync.md; user: "not important right
  # now"). Deprioritized, NOT deleted: capability stays captured in PRD F8/FR-25..29 +
  # the reserved architecture sync seam; LFR-25..29 remain mapped in epics.md. No epic
  # row until/unless it is picked up as a future epic.

  # Epic 8: Reader & annotation polish, round 2 (post-v1, Phase-1.5) — added 2026-07-07
  # correct-course (sprint-change-proposal-2026-07-07-deferred-review.md) AS Epic 9 (one
  # reader-fidelity item, 8-1); RENUMBERED 9 -> 8 on 2026-07-11 (Remote sync un-numbered).
  # BROADENED 2026-07-11 correct-course (sprint-change-proposal-2026-07-11-epic-8-9-stories.md):
  # the epic's "no new capability / no new FRs" charter is retired. From a ten-request user batch
  # (three heavyweight Phase-2 features split into the new Epic 9), it now also holds Annotation-
  # Bank capability (8-2 filter, 8-3 sort), comment-on-region (8-4), a venue-display refinement
  # (8-5), and three reader defects (8-6/8-7/8-8). New reader FRs: FR-23/24/25. No story files
  # yet -> backlog.
  epic-8: in-progress
  8-1-paragraph-aware-copy: done
  8-2-annotation-bank-filter-by-type: done
  8-3-sort-annotations-reading-order: done
  8-4-comment-on-region: done
  8-5-venue-short-name: done
  8-6-comment-preview-size: done
  8-7-tab-switch-resume: done
  8-8-empty-space-drag-no-select: done
  8-9-snap-empty-space-drag-to-text: review
  # 8-11 sequenced BEFORE 8-10: the snap it adds is one of the concerns 8-10's
  # textSelection.ts refactor absorbs (added 2026-07-12, design-spec attempt 2 after 8-9 negative spike).
  8-11-snap-empty-space-drag-to-text-attempt-2: ready-for-dev
  8-10-epic-8-structural-refactor: backlog
  epic-8-retrospective: optional

  # Epic 9: Phase 2 kickoff — reading helper & paper portability (post-v1, Phase-2) — added
  # 2026-07-11 correct-course (sprint-change-proposal-2026-07-11-epic-8-9-stories.md). FIRST
  # epic across the v1 -> Phase-2 line. The three net-new Phase-2 features split out of the
  # ten-request batch: 9-1 download (light, reuses GET /api/docs/{id}/file), 9-2 export annotated
  # PDF (SPIKE-FIRST, client-vs-server flatten decision), 9-3 footnote/reference inline preview
  # (SPIKE-FIRST, the reading-helper through-line). New FRs: reader FR-26 (export), FR-27
  # (preview); Library FR-30 (download). 9-2/9-3 want an architecture-spine note at create-story.
  # No story files yet -> backlog.
  epic-9: backlog
  9-1-download-paper: backlog
  9-2-export-annotated-pdf: backlog
  9-3-footnote-reference-preview: backlog
  epic-9-retrospective: optional

action_items:
  - epic: 1
    action: "Codify a document-level interaction-handler convention in CLAUDE.md (bind key/pointer handlers at document level, phase-gated, exempt editable/buttons) to stop the recurring focus bug"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "When adding any render/ export, update both vi.mock('./render') barrels (App.test, Reader.test) in the same change; note in dev-story checklist/CLAUDE.md"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "Keep cross-model code review (bmad-code-review via Codex) as standing practice for every story"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "Standing principle: don't reinvent wheels, adopt stable solutions over from-scratch; first applied to the Epic 2 anchor layer"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "PREP-1 (critical, before story 2-1): anchor/ primitives spike, adopt pdf.js convertToPdfPoint/convertToViewportPoint + native Selection/getClientRects rather than hand-rolling the anchor math"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "PREP-2: decide the command-stack approach (zundo / immer-patches vs bespoke) before Epic 3 -- RESOLVED 2026-06-30 (Epic 2 retro): zundo"
    owner: "Wonseok"
    status: done
  - epic: 1
    action: "PREP-3: design the transient-overlay state machine once (armed-tool + quick-box + pending/empty), reusing the ToC overlay lessons"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-1 (critical, before 3.2): adopt zundo for the single client command stack (AR-7, in-memory do/undo discarded on reload)"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-2 (sequencing, before 3.1): land the Story 5.0 structural refactor (per-kind dispatch table + overlay FSM consolidation + src module split) at the 2/3 boundary, ahead of 3.1"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-3: every Epic-2 client-only edit (recolor/restroke/retext/resize/delete) converges onto the 3.1 command path; no new client-only annotation mutation added in Epic 3"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-4: doc-scope the store when Epic-3 ownership/persistence lands (hydrate-on-open, flush-on-change, AR-6); stop the per-story defensive cross-doc patches -- 2026-07-02 correct-course: routed to story 5-8-doc-scope-store; closed 2026-07-03 (PR #41 merged)"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-5: live-smoke EVERY geometry/placement feature at DPR>1 before done; backfill the missing Story 2.11 box-select smoke"
    owner: "Wonseok"
    status: in-progress
  - epic: 2
    action: "AE-6 (standing): keep cross-model Codex review on every story; caught HIGH bugs in 2.2/2.5/2.8/2.10 -- 2026-07-02 correct-course: codified in CLAUDE.md (Auto code-review after dev-story)"
    owner: "Wonseok"
    status: done
  - epic: 2
    action: "AE-7: fix the Codex-sandbox backend-pytest friction (UV_CACHE_DIR=/tmp/uv-cache + the TestClient hang) so reviews can run the backend suite -- 2026-07-02 correct-course: documented backend suite as run-it-yourself in CLAUDE.md (Backend-tests Sandbox note + Auto code-review); real root-cause fix left to AE3-4/host"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-1: update sprint-status.yaml at PR-merge time, not batched later -- 2026-07-02 correct-course: codified in CLAUDE.md (BMad workflow)"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-2: fill in every story's Dev Agent Record (model, debug log, completion notes, status) before marking it done -- 2026-07-02 correct-course: codified in CLAUDE.md (BMad workflow)"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-3 (elevated priority): close AE-4 (doc-scope the store) -- no longer purely theoretical, it is the gap behind Story 3.4's HIGH Codex finding -- 2026-07-02 correct-course: routed to story 5-8-doc-scope-store; closed 2026-07-03 (PR #41 merged)"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-4: get a real fix for the Codex-sandbox backend-pytest hang, or document backend verification as run-it-yourself in bmad-code-review -- 2026-07-02 correct-course: documented run-it-yourself in CLAUDE.md (took the documented-fallback branch); real root-cause fix stays open if the hang blocks host runs"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-5: confirm/backfill the DPR>1 live-smoke gaps in Story 2.11 (box-select) and Story 3.3 (cross-page grouped-delete)"
    owner: "Wonseok"
    status: open
  - epic: 3
    action: "AE3-6: add a pre-commit/CI check that pyproject.toml's version and uv.lock's recorded version match -- 2026-07-02 correct-course: added server/tests/test_version.py (runs in the backend suite; no CI/pre-commit framework exists in-repo)"
    owner: "Wonseok"
    status: done
  - epic: 3
    action: "AE3-7 (investigation, no deadline): FR-26/Story 3.8 stays open -- revisit with a resolve-once-on-pointerup anchor strategy, or confirm the Chromium caretRangeFromPoint bug is headless/CDP-only, before re-attempting"
    owner: "Wonseok"
    status: open
  - epic: 6
    action: "AE6-1 (quality, carries AE3-5/AE-5): close the DPR>1 cross-page smoke backfill (2.11 box-select, 3.3 cross-page grouped-delete). Adopt the 6.7 programmatic-Range + trusted-pointerup path as the STANDARD for cross-page selection smoke (coordinate-drag is not reliably automatable across a scrolled multi-page boundary); fold into [[drag-tools-dont-create-text-selection]]"
    owner: "Wonseok"
    status: open
  - epic: 6
    action: "AE6-2 (tooling): resolve the recurring claude-in-chrome extension unavailability (6.4/6.5/6.6/6.7 fell back; 6.5 lost its browser-visual pass), or standardize smoke on Playwright/chrome-devtools MCP so no story loses coverage"
    owner: "Wonseok"
    status: open
  - epic: 6
    action: "AE6-3 (process): reconcile model-per-job with reality -- follow Sonnet 5 xHigh for dev-story, or update CLAUDE.md if Opus 4.8 is the intended default (6.5/6.8 ran on Opus against a Sonnet recommendation without a decision)"
    owner: "Wonseok"
    status: done
  - epic: 6
    action: "AE6-4 (process/doc): when a mid-epic correct-course changes a shipped gesture (6.7 hover-Open superseded double-click-to-open), reconcile epics.md story text to what shipped, not only the story file"
    owner: "Wonseok"
    status: open
  - epic: 6
    action: "AE6-5 (prep, Epic 7): Story 7.5 (Trash) must honor the AL-4 point-4 re-upload-restores-trashed edge that 6.2/6.4 deferred to it; flag in the 7.5 story spec at create-story time"
    owner: "Wonseok"
    status: done
  - epic: 7
    action: "AE7-1 (process, resolves AE6-3): next bmad-dev-story runs on Sonnet 5 xHigh per the standing CLAUDE.md recommendation; the Opus drift (6.5/6.8/7.12) stops. CLAUDE.md unchanged"
    owner: "Wonseok"
    status: open
  - epic: 7
    action: "AE7-2 (tooling, carries AE6-2): fix claude-in-chrome extension reliability (down again in the 7-12 smoke); keep it as the preferred smoke driver and invest in the connection, do NOT switch the toolchain to Playwright"
    owner: "Wonseok"
    status: open
  - epic: 7
    action: "AE7-3 (process/doc, carries AE6-4): when a mid-epic correct-course changes shipped scope/behavior, reconcile epics.md in the SAME change, not only the story file. Recurred across Epics 3/6/7 (the 7-12 Reality-corrections being latest)"
    owner: "Wonseok"
    status: open
  - epic: 7
    action: "AE7-4 (quality, carries AE6-1): DPR>1 cross-page smoke backfill (2.11 box-select, 3.3 cross-page grouped-delete) still unconfirmed; no Epic-7 occasion. Carry to the next epic touching cross-page PDF selection (Epic 8 reader-fidelity, renumbered from Epic 9 on 2026-07-11); adopt programmatic-Range + trusted-pointerup as the standard then"
    owner: "Wonseok"
    status: open
  - epic: 7
    action: "AE7-5 (pattern to repeat): adopt refactor-as-the-last-story-of-the-epic as the default for epics that accrue structural debt; 7-12 proved one terminal byte-identical-contract decomposition beats five partial per-feature refactors"
    owner: "Wonseok"
    status: open
