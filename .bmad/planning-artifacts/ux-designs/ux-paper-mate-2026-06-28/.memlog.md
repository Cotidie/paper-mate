---
topic: Paper Mate UX (Phase 1 viewer/annotator)
updated: 2026-06-28T05:04
---

- (decision) DESIGN.md handling: KEEP all token scales as-is; RETARGET component layer from Expo marketing (hero/pricing/device-mockup) to Paper Mate reader components; update frontmatter name/description. Per arg 'largely reuse DESIGN.md for styling'.
- (decision) Working mode: Coaching path, wireframes woven in. Final spines (retargeted DESIGN.md + new EXPERIENCE.md) land at REPO ROOT as the live contract CLAUDE.md points to.
- (decision) Form-factor: web desktop, Chrome+Firefox (from PRD). Mobile out.
- (decision) Reader feel: Obsidian-style minimal chrome. Left rail modeled on Kami toolbar. Both rail and Annotation Bank OVERLAY the canvas (float on top); canvas position/size never changes (NFR-1). Rail is collapsible, sits over canvas not in a gutter.
- (decision) IA: S0 empty/open state (file picker + drag-drop), S1 Reader (single surface = rail overlay + fixed PDF canvas + Bank overlay), transient drag-to-change-tool picker popup. v1 Phase 1 only.
- (decision) Zoom inputs: ctrl +/-, bottom-right buttons, ctrl+scroll, live % readout.
- (decision) Save model: autosave, debounced, 'Saved/Saving' indicator, no save button (NFR-4 durability).
- (decision) Tool persistence: armed tool STAYS armed for multiple annotations until switched; rail does NOT auto-collapse after pick.
- (decision) Keyboard shortcuts wanted (power user) for tools, undo/redo, delete, zoom, panel toggles, page nav.
- (change) Quick box is CONTEXTUAL per active mode, not one universal picker. Selection mode -> tool-type picker (H/U/comment/memo). Armed-tool mode -> tool-tuned quick box: highlight/underline->color picker; pen->color+stroke; memo->inline text+color/size; comment->note bubble opens directly; box-select->region tool picker. All float, never shift canvas.
- (change) Comment redefined: comment highlights the underlying text AND shows an anchored comment bubble icon to mark it as a comment (vs plain highlight). Updates PRD FR-11/IP-10.
- (decision) Hotkey map confirmed as proposed. Key-flow protagonist = Wonseok (actual user).
- (event) Spines authored at repo root: DESIGN.md (retargeted: tokens kept, +annotation palette, reader components) + EXPERIENCE.md (new). Reader S1 key-screen mock -> mockups/reader-mock.html.
- (assumption) Mock coverage: S1 Reader mocked. S0 empty-state, quick-box variants, ToC = spine-only (simple, solo tool). Log if any need visual ref.
- (event) spines finalized
